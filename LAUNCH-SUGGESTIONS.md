# خضارك — خطة ما قبل الدعاية

مراجعة للكود والمنتج بعد تنفيذ التعديلات الستة، مرتّبة حسب الأولوية.
كل بند مكتوب بحيث تقدر تنفّذه (أو تطلب مني أنفّذه) مباشرة.

---

## الأولوية القصوى — قبل ما تصرف أول ريال على إعلان

### 1. التتبّع والقياس (بدونه الإعلان أعمى)

حالياً ما في أي أداة تحليلات في المشروع. لو أطلقت حملة الآن، ما راح تعرف
أي إعلان جاب اشتراك وأي واحد أحرق ميزانية.

- **GA4 + Meta Pixel + TikTok Pixel** عبر `next/script` مع `strategy="afterInteractive"`.
- **أحداث تحويل مخصّصة**: `add_to_cart` عند الضغط على زر الإضافة،
  `begin_checkout` عند دخول `/subscription`، و`purchase` عند نجاح الدفع في
  `/subscription/confirmed/[id]` مع قيمة الاشتراك.
- **Conversions API من السيرفر** (Meta + TikTok) من داخل
  `app/api/webhooks/moyasar/route.ts` — بهذي الطريقة التحويل يُسجَّل حتى لو
  المستخدم عنده حاجب إعلانات، والبيانات تصير أدق بكثير من البكسل لوحده.
- **PostHog أو Microsoft Clarity** لتسجيل الجلسات — أول ٥٠ جلسة راح تكشف لك
  أين ينقطع الناس في الـ wizard أكثر من أي تحليل نظري.
- `utm_*` محفوظة في cookie عند أول زيارة، ومرسلة مع إنشاء الاشتراك، عشان
  تربط الاشتراك بالحملة اللي جابته.

### 2. الصفحات القانونية والثقة (السعودية تحديداً)

الزائر السعودي يبحث عن هذي قبل ما يدخل بطاقته، وغيابها يرفع تكلفة الاكتساب:

- `/terms` الشروط والأحكام، `/privacy` سياسة الخصوصية، `/refund` سياسة
  الاستبدال والاسترجاع (ذكرتها في الأسئلة الشائعة لكنها تحتاج صفحة مستقلة).
- **رقم السجل التجاري والرقم الضريبي** في الفوتر — مطلوب نظاماً لأي متجر
  إلكتروني، وشعار «معروف» إذا كنت مسجّل.
- **صفحة تواصل** فيها واتساب مباشر (`wa.me`) — نسبة كبيرة من العملاء
  السعوديين يفضّلونه على البريد.
- **زر واتساب عائم** في الجوال؛ أثره على التحويل في هذا القطاع ملموس.

### 3. صور المنتجات

الصفحة الرئيسية الجديدة تعرض صور المنتجات من قاعدة البيانات مباشرة — يعني
جودة الصور صارت هي واجهة الموقع. صورة موبايل بإضاءة سيئة راح تهدم كل
التصميم. أنصح بجلسة تصوير واحدة: خلفية موحّدة، إضاءة طبيعية، مربّعة
(1:1)، ١٢٠٠×١٢٠٠ بكسل على الأقل.

### 4. متغيّر `NEXT_PUBLIC_SITE_URL`

أضفت `app/sitemap.ts` و`app/robots.ts` وهما يقرآن هذا المتغيّر (وإلا
يستخدمان `https://khodarak.com` كافتراضي). اضبطه في Vercel وفي
`.env.example` قبل النشر، وأضف `metadataBase` في `app/layout.tsx` عشان
روابط OpenGraph تطلع مطلقة.

---

## أولوية عالية — أول أسبوعين

### 5. سيو عربي حقيقي

- **JSON-LD**: `Organization` و`WebSite` في الـ layout، `Product` في
  `/browse/[id]`، و`FAQPage` في الرئيسية — قسم الأسئلة الشائعة الجديد جاهز
  للاستفادة منه، ويعطيك نتائج غنية في جوجل.
- **صورة OpenGraph**: `app/opengraph-image.tsx` — بدونها مشاركة الرابط في
  واتساب تطلع بدون صورة، وهذا يقتل نسبة النقر.
- **روابط المنتجات حالياً UUID** (`/browse/1f2e...`). الأفضل
  `/browse/tamatim-baladi` — يحتاج عمود `slug` في جدول `products` مع
  migration بسيط.
- `generateMetadata` في `/browse/[id]` — حالياً كل صفحات المنتجات ترث
  نفس العنوان والوصف.
- **صفحات هبوط للمدن**: `/riyadh` و`/jeddah` — «توصيل خضار طازج في الرياض»
  كلمة مفتاحية بحجم بحث حقيقي ومنافسة أقل بكثير من العام.

### 6. تحسينات التحويل

- **عرض أول صندوق**: كوبون خصم للطلب الأول. يحتاج جدول `coupons` وحقل في
  محرك التسعير — أكبر رافعة تحويل متاحة لك.
- **زر CTA ثابت في الجوال** داخل `/subscription` — حالياً ملخص الطلب يصير
  تحت المحتوى في الشاشات الصغيرة، والمستخدم يحتاج ينزل عشان يكمل.
- **الأرقام في الـ Hero حالياً تسويقية** (`+٢٤٠٠ صندوق`، `٤.٩/٥`). إما
  تستبدلها بأرقامك الحقيقية أو تشيلها — إعلان بأرقام غير صحيحة مخاطرة
  نظامية وسمعة.
- **آراء العملاء** في `Testimonials.tsx` نصوص مكتوبة مسبقاً. أول عشرة عملاء
  حقيقيين، بأسمائهم وبإذنهم، أقوى بكثير.
- **استعادة السلة المتروكة**: من ترك سلة فيها منتجات ولم يشترك خلال ٢٤
  ساعة، أرسل له بريد أو رسالة تذكير. جدول `cart_items` موجود أصلاً، تحتاج
  فقط cron job إضافي على نمط `app/api/cron/renewals`.

### 7. الإشعارات

لا يوجد حالياً أي إشعار للعميل خارج الموقع:

- **بريد**: تأكيد الاشتراك، تذكير قبل التوصيل بيوم، فشل الدفع، إيصال
  التجديد (Resend أو Supabase Auth SMTP).
- **رسائل نصية أو واتساب** (Unifonic أو Twilio): «صندوقك في الطريق» — أعلى
  أثر على رضا العميل في خدمات التوصيل.
- **إشعار داخلي لك**: كل اشتراك جديد يوصل على واتساب أو سلاك عشان تلحق على
  التجهيز.

---

## أولوية متوسطة — البنية التقنية

### 8. بعد حذف GitHub Actions

حذفت `ci.yml` كما طلبت (نُقل إلى `_to_delete/`). انتبه أن هذا ألغى شبكتي
أمان: التحقق قبل الدمج، ونشر Docker إلى الخادم.

- إذا كنت تنشر عبر **Vercel Git integration**، النشر لسه شغّال ومافي مشكلة.
- إذا كنت تعتمد على وظيفة `deploy` (Docker + SSH) في ذلك الملف، النشر
  التلقائي توقف الآن.
- عوّض التحقق بـ **git hook محلي** (`husky` + `lint-staged`) يشغّل
  `npm run typecheck && npm run lint` قبل كل commit. هذا أخف من CI ويمسك
  ٩٠٪ من الأخطاء نفسها.

### 9. رصد الأخطاء والتشغيل

- **Sentry** (`@sentry/nextjs`) — بدونه، خطأ في الدفع عند عميل حقيقي ما
  راح تعرف عنه إلا لما يشتكي.
- **مراقبة توفّر** على `/api/health` (Better Stack أو UptimeRobot).
- **تنبيه على `/api/cron/renewals`**: هذا أهم job في النظام — لو فشل بصمت،
  التجديدات تتوقف وما راح تلاحظ إلا بعد أيام.
- **نسخ احتياطي**: فعّل Point-in-Time Recovery في Supabase (غير متاح في
  الخطة المجانية — يستاهل الترقية قبل أول عميل يدفع).

### 10. الأمان

- **تحديد معدل الطلبات (rate limiting)** على `/api/*`، خصوصاً تسجيل
  الدخول وإنشاء الحساب — Supabase عنده حد افتراضي لكنه غير كافٍ.
- **رؤوس أمان** في `next.config.ts`: `Content-Security-Policy`،
  `Strict-Transport-Security`، `X-Content-Type-Options`،
  `Referrer-Policy`. صفحة بها نموذج دفع بدون CSP نقطة ضعف واضحة.
- **`middleware.ts` صار deprecated في Next 16** — البناء يطبع تحذيراً
  ويقترح إعادة تسميته إلى `proxy.ts`. التحويل بأمر واحد:
  `npx @next/codemod@canary middleware-to-proxy .`
- **تأكيد البريد الإلكتروني** — تأكّد أنه مفعّل في إعدادات Supabase Auth،
  وإلا أي أحد يسجّل بأي بريد.

### 11. الأداء (نُفّذ جزء منه)

ما تم فعلياً في هذي الجولة:

- قراءات الكتالوج انتقلت إلى عميل Supabase مجهول قابل للتخزين المؤقت
  (`lib/supabase/publicCatalog.ts`) بنافذة ٦٠ ثانية، مع `revalidateTag`
  فوري عند تعديل الأدمن. عملياً: ألف زائر = استعلام واحد بدل ألف.
- `formats: ["image/avif", "image/webp"]` مع `minimumCacheTTL` ٣٠ يوم.
- `optimizePackageImports` لـ `lucide-react` و`@reduxjs/toolkit`.
- `prefetch` على روابط التنقّل الرئيسية.

ما تبقّى ويستاهل:

- **استضافة الخط ذاتياً** (`next/font/local`) بدل Google Fonts —
  IBM Plex Sans Arabic ثقيل، والاستضافة الذاتية تشيل اتصال DNS خارجي من
  المسار الحرج للـ LCP.
- **Partial Prerendering** — الرئيسية الآن ديناميكية بالكامل لأن `TopNav`
  يقرأ الكوكيز. مع PPR يصير الهيكل ثابتاً والهيدر وحده ديناميكي.
- تشغيل Lighthouse على الجوال (حذفت `lighthouserc.json` مع ملفات CI)
  والاستهداف: LCP أقل من ٢٫٥ ثانية على شبكة 4G.

---

## أولوية أقل — لكن تصنع الفرق

### 12. تجربة المستخدم

- **صناديق جاهزة**: «صندوق العائلة»، «صندوق السلطة»، «صندوق العصائر» —
  اختيار جاهز بضغطة بدل ما يبني المستخدم صندوقه من الصفر. أكبر نقطة احتكاك
  عندك حالياً هي أن الاشتراك يتطلب قرارات كثيرة قبل أول شراء.
- **بحث في الكتالوج** — لا يوجد حقل بحث في `/browse`، فقط تصنيفات وفلاتر.
- **الحالات الفارغة** في لوحة التحكم لعميل بدون اشتراك: أضف اقتراح صندوق
  جاهز بدل زر واحد.
- **إشعارات Toast** — التحديث المتفائل صار فوريّاً، لكن لو فشل طلب (شبكة
  مقطوعة مثلاً) القيمة ترجع بصمت. Toast صغير «تعذر التحديث» يمنع الحيرة.
- **الوضع الليلي** — غير موجود إطلاقاً (`tailwind.config.ts` فيه لوح ألوان
  فاتح فقط). ليس أولوية، لكنه متوقَّع.

### 13. لوحة الأدمن

- **بحث وفرز في جدول المنتجات** — يصير مؤلماً بعد ٥٠ منتج.
- **تعديل جماعي**: «إخفاء كل الفواكه» بضغطة.
- **قائمة تجهيز يومية**: مجموع الكميات لكل منتج لتوصيلات اليوم — هذا ما
  يحتاجه المستودع فعلياً كل صباح، ولا يوجد حالياً.
- **تصدير CSV** للطلبات والاشتراكات.

### 14. تغطية الاختبارات

- اختبارات الوحدة ممتازة (١٦٠ اختبار ناجح) لكنها تغطي المنطق النقي فقط.
- اختبارات e2e لا تغطي: صفحة `/cart` الجديدة، تحويل الأدمن بعد تسجيل
  الدخول، والتراجع عن التحديث المتفائل عند فشل الشبكة.
- `tests/e2e/catalog-cart.spec.ts` يتوقّع نص يطابق `/\d+ منتج/` في شريط
  السلة، وهذا النص لم يعد موجوداً (الشارة صارت رقماً فقط). الاختبار يفشل
  حالياً لسبب سابق لهذي التعديلات، ويحتاج تحديث.

---

## ملاحظة تنظيفية

`device_bash` لا يملك صلاحية حذف على مجلداتك، فنقلت الملفات المحذوفة إلى
`_to_delete/` بدل حذفها:

- `ci.yml` و`lighthouserc.json` (حذف GitHub Actions — طلبك رقم ٥)
- `ProductRowActions.tsx` و`OrderRowActions.tsx` (استُبدلا بجداول متفائلة)
- `khodarak-src.tgz` و`khodarak-src2.tgz` (أرشيفات مؤقتة استخدمتها للتحقق
  من البناء والأنواع)

احذف المجلد `_to_delete` كاملاً من الويندوز.
